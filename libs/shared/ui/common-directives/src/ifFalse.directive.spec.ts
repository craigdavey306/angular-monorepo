import { IfFalseDirective } from './ifFalse.directive';

describe('IfFalseDirective', () => {
  it('should create an instance', () => {
    const directive = new IfFalseDirective();
    expect(directive).toBeTruthy();
  });
});
